# Optional: Vercel KV (Redis) for persistent caching across deployments
# Set these in your Vercel project dashboard after creating a KV database.
# Without these, the app uses in-memory caching (resets on server restart).
#
# KV_URL=redis://...
# KV_REST_API_URL=https://...
# KV_REST_API_TOKEN=...
